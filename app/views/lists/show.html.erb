<div class="row">
  <div class="col-md-12">
    <h2 class="title"><%= @list.name %></h2>

    <%= link_to 'Custom Fields', list_custom_fields_path(@list), class: 'btn btn-default' %>
    <%= link_to 'Add subscribers', new_list_import_path(@list), class: 'btn btn-default' %>
  </div>
</div>
