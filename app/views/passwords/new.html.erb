<div class="l-container-minimal">
  <h2><%= page_title %></h2>
  <p><%= t('.description') %></p>

  <%= simple_form_for :password, url: passwords_path do |f| %>
    <%= f.input :email, label: false %>
    <%= f.button :submit, class: 'btn-success' %>
  <% end %>
</div>
