---
en:
  activerecord:
    errors:
      models:
        account:
          invalid_token: 'AWS access key ID provided does not exist'
          incorrect: 'is incorrect'
        campaign:
          unverified_email_address: 'is not verified'
          unverified_domain: 'domain is not verified'
          unstructured_from: 'has forbidden characters'
  activemodel:
    errors:
      models:
        delivery:
          attributes:
            base:
              quota_exceeded: 'Quota exceeded.'
